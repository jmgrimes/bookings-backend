extend schema
  @link(url: "https://specs.apollo.dev/link/v1.0", import: ["@link"])
  @link(url: "https://specs.apollo.dev/federation/v2.7", import: ["@shareable"])

type PayloadError @shareable {
  message: String!
}
